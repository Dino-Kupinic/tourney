export default defineAppConfig({
  app: {
    head: {
      viewport: "width=device-width, initial-scale=1, maximum-scale=1",
    },
  },
  ui: {
    colors: {
      primary: "blue",
      neutral: "neutral",
    },
  },
})
